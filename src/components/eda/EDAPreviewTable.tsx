import type { EDAPreview } from "@/types/eda";
export default function EDAPreviewTable({ preview, onPage }: { preview: EDAPreview; onPage: (page: number) => void }) {
  return <div className="overflow-hidden rounded-xl border border-slate-800"><div className="max-h-[460px] overflow-auto"><table className="min-w-full text-sm"><thead className="sticky top-0 bg-slate-950"><tr><th className="px-3 py-2 text-left text-slate-500">#</th>{preview.columns.map((column) => <th key={column} className="whitespace-nowrap px-3 py-2 text-left text-slate-300">{column}</th>)}</tr></thead><tbody>{preview.rows.map((row, index) => <tr key={(preview.page - 1) * preview.page_size + index} className="border-t border-slate-800"><td className="px-3 py-2 text-slate-500">{(preview.page - 1) * preview.page_size + index + 1}</td>{preview.columns.map((column) => { const value = row[column]; return <td key={column} title={value == null ? "Null" : String(value)} className="max-w-64 truncate whitespace-nowrap px-3 py-2 text-slate-300">{value == null || value === "" ? <span className="italic text-slate-600">null</span> : String(value)}</td>; })}</tr>)}</tbody></table></div><div className="flex items-center justify-end gap-3 border-t border-slate-800 p-3 text-xs text-slate-400"><span>Page {preview.page} of {preview.pages}</span><button disabled={preview.page <= 1} onClick={() => onPage(preview.page - 1)} className="rounded border border-slate-700 px-2 py-1 disabled:opacity-30">Previous</button><button disabled={preview.page >= preview.pages} onClick={() => onPage(preview.page + 1)} className="rounded border border-slate-700 px-2 py-1 disabled:opacity-30">Next</button></div></div>;
}
