import type { EDAProfiles } from "@/types/eda";
export default function EDAColumns({ data }: { data: EDAProfiles | null }) {
  if (!data) return <p className="py-16 text-center text-slate-400">Calculating resilient profiles for every column…</p>;
  return <div className="overflow-auto rounded-xl border border-slate-800"><table className="min-w-full text-sm"><thead className="bg-slate-950"><tr>{["Column", "Type", "Non-null", "Missing", "Unique", "Mean / top value", "Outliers"].map((item) => <th key={item} className="whitespace-nowrap px-4 py-3 text-left text-slate-400">{item}</th>)}</tr></thead><tbody>{data.profiles.map((profile) => <tr key={profile.name} className="border-t border-slate-800"><td className="px-4 py-3 font-medium text-white">{profile.name}<div className="text-xs text-slate-500">{profile.dtype}</div></td><td className="px-4 py-3 text-blue-300">{profile.semantic_type}</td><td className="px-4 py-3 text-slate-300">{profile.non_null_count.toLocaleString()}</td><td className="px-4 py-3 text-slate-300">{profile.null_count.toLocaleString()} ({profile.missing_percentage}%)</td><td className="px-4 py-3 text-slate-300">{profile.unique_count.toLocaleString()} ({profile.unique_percentage}%)</td><td className="max-w-64 truncate px-4 py-3 text-slate-300">{String(profile.mean ?? profile.most_frequent_value ?? "—")}</td><td className="px-4 py-3 text-slate-300">{String(profile.potential_outlier_count ?? "—")}</td></tr>)}</tbody></table></div>;
}
