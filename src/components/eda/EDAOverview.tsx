import { AlertTriangle, Columns3, Copy, FileSpreadsheet, HardDrive, Rows3 } from "lucide-react";
import type { EDAOverview as Overview, EDAPreview } from "@/types/eda";
import EDAPreviewTable from "./EDAPreviewTable";
const Card = ({ label, value, icon }: { label: string; value: string | number; icon: React.ReactNode }) => <div className="rounded-xl border border-slate-800 bg-slate-950 p-4"><div className="text-blue-400">{icon}</div><p className="mt-3 text-xs text-slate-400">{label}</p><p className="mt-1 text-xl font-semibold text-white">{value}</p></div>;
export default function EDAOverview({ overview, preview, onPreviewPage }: { overview: Overview; preview: EDAPreview; onPreviewPage: (page: number) => void }) {
  const p = overview.project;
  return <div className="space-y-6"><div><h2 className="text-xl font-semibold text-white">{p.original_filename}</h2><p className="mt-1 text-sm text-slate-400">Uploaded {new Date(p.created_at).toLocaleString()} · {p.extension.slice(1).toUpperCase()}</p></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><Card label="Rows" value={p.rows.toLocaleString()} icon={<Rows3 size={20} />} /><Card label="Columns" value={p.columns} icon={<Columns3 size={20} />} /><Card label="Missing cells" value={`${overview.missing_values.toLocaleString()} (${overview.missing_percentage}%)`} icon={<AlertTriangle size={20} />} /><Card label="Duplicates" value={overview.duplicate_rows.toLocaleString()} icon={<Copy size={20} />} /><Card label="Memory" value={overview.memory_usage} icon={<HardDrive size={20} />} /><Card label="File size" value={`${(overview.file_size / 1024 / 1024).toFixed(2)} MB`} icon={<FileSpreadsheet size={20} />} /></div><div className="rounded-xl border border-slate-800 bg-slate-950 p-4"><h3 className="font-semibold text-white">Column structure</h3><div className="mt-3 flex flex-wrap gap-2">{overview.columns.map((column) => <span key={column.name} className="rounded-full bg-slate-800 px-3 py-1 text-xs text-slate-300">{column.name} <span className="text-blue-300">· {column.semantic_type}</span> <span className="text-slate-500">· {column.dtype}</span></span>)}</div></div><div><h3 className="mb-3 font-semibold text-white">Data preview</h3><EDAPreviewTable preview={preview} onPage={onPreviewPage} /></div></div>;
}
